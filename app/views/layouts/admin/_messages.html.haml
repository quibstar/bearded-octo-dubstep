- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div{:id => "main-alert", :class => "alert alert-#{name}"}
      %a.close{"data-dismiss" => "alert"} ×
      = content_tag :div, msg, :id => "flash_#{name}"
